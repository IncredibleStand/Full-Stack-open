{
  "name": "bloglist",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "build": "pnpm --dir client build",
    "start": "NODE_ENV=production pnpm --dir server start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-error-boundary": "^6.1.2"
  }
}